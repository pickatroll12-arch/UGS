name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run unit test suites
        run: npm test

  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install dependencies
        run: npm install
      - name: Install Chromium for Playwright
        run: npx playwright install --with-deps chromium
      - name: Run browser smoke tests
        run: npm run smoke

  handoff:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - name: Append Codex handoff for Kimi
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path('AGENTIC_REVIEW.md')
          text = path.read_text(encoding='utf-8')
          heading = '## 13.2 Handoff de implementación — Codex → Kimi K3'
          if heading not in text:
              handoff = r'''

---

## 13.2 Handoff de implementación — Codex → Kimi K3

> **Estado de Codex: IMPLEMENTADO EN RAMA; pendiente de revisión rector/build tester/owner.**
> Alcance ejecutado bajo el `GO` limitado de §13.1. Stage 2 continúa en `NO-GO`.

### Observación

`SPRITE-01` ya está implementado en la rama `codex/sprite-01` y presentado en
el PR borrador #18. No se modificaron datos, simulación, navegación, editor,
guardado ni `Feedback humano`.

### Evidencia

- Se conservaron intactos los tres PNG originales del owner.
- Se añadieron derivados web transparentes y recortados:
  `pawn_front.png` (82×128), `pawn_side.png` (63×128) y
  `pawn_back.png` (84×128).
- Se eliminaron el fondo gris, la sombra horneada, el texto `Soldado` y el
  artefacto inferior; el renderer mantiene su propia sombra.
- `src/render.js` carga los tres assets una sola vez, conserva el placeholder
  vectorial durante la carga o ante error, y fuerza un único repintado al
  terminar las solicitudes para respetar el render-on-demand.
- La pose se elige con la dirección mundial: sur→frente, norte→espalda,
  este/oeste→lateral, con espejo horizontal para oeste.
- La vista `topDown` conserva el disco de REV3; los sprites solo se usan en las
  dos vistas isométricas.
- El anillo de selección, el bob de movimiento, la escala por zoom y el anclaje
  a los pies permanecen activos.
- `tests/smoke/smoke.mjs` verifica que los tres assets resuelven y cargan.
- `npm test` y `npm run smoke` pasaron en la ejecución CI #31 del PR #18.
- La revisión visual automatizada confirmó las cuatro orientaciones cardinales,
  el espejo oeste, la sombra y el anillo de selección.

### Riesgo residual

Los assets son poses estáticas, no hojas de animación. La comprobación visual
automatizada valida el renderer aislado, pero todavía corresponde al build
tester confirmar tamaño, legibilidad y sensación dentro del flujo jugable real.

### Recomendación

Revisar el PR #18 y ejecutar una pasada humana breve en `isoTilted` e
`isoFlat`, incluyendo movimiento, selección, zoom y un asset ausente simulado
para confirmar el fallback. No interpretar este brief gráfico como autorización
para Stage 2.

### Archivos funcionales afectados

- `Sprites/Placeholders/processed/pawn_front.png`
- `Sprites/Placeholders/processed/pawn_side.png`
- `Sprites/Placeholders/processed/pawn_back.png`
- `src/render.js`
- `tests/smoke/smoke.mjs`
- `AGENTIC_REVIEW.md` (solo este handoff)

### Pruebas necesarias / ejecutadas

- `npm test` — PASS.
- `npm run smoke` — PASS, incluyendo carga de los tres PNG.
- Revisión visual cardinal — PASS.
- Revisión manual del build tester — PENDIENTE.

### Decisión pendiente del rector

Kimi: emitir `GO` o `NO-GO` para pasar el PR #18 a revisión/merge después de
la validación humana. Codex no inicia otro brief mientras esta decisión siga
abierta.
'''
              path.write_text(text.rstrip() + handoff + '\n', encoding='utf-8')
          PY
      - name: Commit handoff
        run: |
          git config user.name 'Codex'
          git config user.email 'codex@users.noreply.github.com'
          git add AGENTIC_REVIEW.md
          git commit -m 'Record SPRITE-01 rector handoff'
          git push origin HEAD:${{ github.head_ref }}
